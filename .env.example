# Server-side only. Never prefix this key with VITE_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna
PORT=3001

# Optional anonymous product analytics.
# The PostHog project key is a public client key; never place private secrets in VITE_ variables.
VITE_POSTHOG_KEY=
VITE_POSTHOG_HOST=https://us.i.posthog.com
